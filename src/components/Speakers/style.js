import {makeStyles} from '@material-ui/core'

const useStyles = makeStyles({
    centersection:{
        display:'flex',
        justifyContent:'center',
        alignItems:'center'
    }
})

export {useStyles} 